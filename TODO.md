# Fix Audio Autoplay Issue

## Plan Steps:

1. [x] Add hidden <audio ref={audioRef}> element to WeddingInvitation.jsx root so ref works and auto-plays on mount.
2. [x] Update index.html: Remove HTML <audio> to prevent conflicts/duplicates.
3. [ ] Test: Run `npm run dev`, open app, check if audio plays automatically (allow if browser prompts), check console for errors.
4. [ ] Complete task.

Current progress: Completed steps 1-2. Proceeding to test.
